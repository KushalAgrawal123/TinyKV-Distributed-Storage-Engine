# TinyKV default configuration
# Format: key=value, one per line. Lines starting with # are comments.

port=6380
bind=0.0.0.0

# DEBUG, INFO, WARN, or ERROR
log_level=INFO

# Maximum number of keys before LRU eviction kicks in. 0 = unbounded.
max_keys=0
